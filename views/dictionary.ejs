<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Number Facts</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f9fa;
            color: #343a40;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }

        h1 {
            text-align: center;
            margin-bottom: 30px;
        }

        form {
            text-align: center;
        }

        input[type="text"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ced4da;
            border-radius: 5px;
        }

        button[type="submit"] {
            background-color: #860e29;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        button[type="submit"]:hover {
            background-color: #A82C48;
        }

        .result-box {
            margin-top: 20px;
        }

        /* Navbar Styles */
        .navigation-wrap {
            background-color: #A82C48;
        }

        .navbar-brand img {
            max-height: 50px;
        }

        .navbar-nav .nav-link {
            color: #fff !important;
            margin-left: 15px;
            margin-right: 15px;
        }

        .navbar-nav .nav-link:hover {
            color: #ffc107 !important;
        }

        .btn-primary {
            background-color: #A82C48;
            border-color: #A82C48;
        }

        .btn-primary:hover {
            background-color: #860e29;
            border-color: #860e29;
        }

        .modal-content {
            background-color: #f8f9fa;
            border-radius: 10px;
            box-shadow: 0px 0px 20px 0px rgba(0, 0, 0, 0.1);
            padding: 20px;
        }

        .modal-open .modal {
            overflow-y: auto;
        }

        .custom-light {
            color: #f0f0f0;
        }

        select {
            padding: 10px;
            font-size: 16px;
            border: 1px solid #ccc;
            border-radius: 5px;
            width: 100%;
            box-sizing: border-box;
            margin-bottom: 10px;
        }

        body {
            margin: 0;
            margin-bottom: 20px;
            padding: 0;
            height: 100%;
            display: flex;
            flex-direction: column;
        }

        footer {
            background-color: #A82C48;
            color: #fff;
            padding: 20px 0;
            text-align: center;
            width: 100%;
            position: fixed;
            bottom: 0;
        }

        footer p {
            margin: 0;
        }

        .content {
            flex: 1;
        }

        .result-container {
            background-color: #fff;
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 100%;
            margin-bottom: 80px;
        }

        .card {
            background-color: #fff;
            border: 1px solid #ccc;
            border-radius: 8px;
            padding: 15px;
            margin-bottom: 15px;
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
        }

        .card h3 {
            color: #A82C48;
            margin-bottom: 10px;
        }

        .card p {
            margin: 0;
        }

    </style>
</head>
<body>
<header>
    <nav class="navbar navbar-expand-lg navigation-wrap">
        <div class="container">
            <li class="nav-item">
                <a class="fas fa-home custom-light nav-link" href="/views/login.ejs"></a>
            </li>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="/weather/<%= userId %>">Weather</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/number/<%= userId %>">Number</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/translate/<%= userId %>">Translate</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/dictionary/<%= userId %>">Dictionary</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</header>
<div class="container">
    <h1>Search from dictionary</h1>
    <form action="/dictionary/<%= userId %>" method="post" style="margin-bottom: 20px">
        <input type="text" name="text" placeholder="Enter text to translate">
        <select name="lang">
            <option value="en-fr">English to French</option>
            <option value="en-es">English to Spanish</option>
            <option value="en-de">English to German</option>
            <option value="en-ru">English to Russian</option>
        </select>
        <button type="submit">Search</button>
    </form>
    <div class="result-container">
        <% if (translationData) { %>
            <h2 style="text-align: center; color: #A82C48;">Translation Result for: <%= translationText %></h2>
            <% translationData.forEach((entry, index) => { %>
                <div class="card">
                    <h3>Word: <%= entry.text %></h3>
                    <p><strong>Part of Speech:</strong> <%= entry.pos %></p>
                    <p><strong>Translation:</strong> <%= entry.translation %></p>
                </div>
            <% }); %>
        <% } else { %>
            <p style="text-align: center;">No translation found for the given text.</p>
        <% } %>
    </div>
</div>
</body>
<footer>
    <div class="container">
        <p>Dinmukhammed Mynzhasar | SE-2207</p>
    </div>
</footer>

</html>
